# ====================================
# Auth.js 认证配置（必需）
# ====================================

# Auth Secret - 用于加密 session 和 token
# 生成方式：在终端运行 `openssl rand -base64 32`
# 或使用在线工具：https://generate-secret.vercel.app/32
AUTH_SECRET="your-generated-secret-here"

# Auth URL - 应用的完整 URL
# 本地开发：http://localhost:2011
# 生产环境：https://your-domain.com
# ⚠️ 重要：在 Vercel 部署时，需要在环境变量中区分配置：
#   - Preview 环境：使用预览域名（如 https://your-app-git-branch.vercel.app）
#   - Production 环境：使用生产域名（如 https://your-domain.com）
AUTH_URL="http://localhost:2011"

# Google OAuth 配置
# 在 Google Cloud Console 创建 OAuth 2.0 凭据
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# GitHub OAuth 配置
# 在 GitHub Settings > Developer settings > OAuth Apps 创建新应用
# https://github.com/settings/developers
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# ====================================
# 数据库配置（必需）
# ====================================

# Neon PostgreSQL 数据库连接
# 从 Vercel 项目的 Storage > Neon 获取这两个 URL

# 直接连接 - 用于 Prisma Migrate（数据库迁移）
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname"

# 池化连接（可选）- 用于应用查询，提供更好的连接复用
# 如果 Neon 提供了 pooled URL，建议在生产环境使用
# DATABASE_URL_POOLED="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname"

# ====================================
# GitHub API 配置（可选）
# ====================================

# GitHub Personal Access Token - 用于提高 GitHub API 访问速率
# 未认证：60 次/小时，认证：5000 次/小时
# 创建方式：https://github.com/settings/tokens
# 权限：不需要勾选任何权限（只读公开数据）
# GITHUB_TOKEN="ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# ====================================
# 代理配置（可选）
# ====================================

# 如果需要通过代理访问外部服务，请配置以下变量
# 注意：需要在启动时设置 ENABLE_PROXY=true 或使用 npm run dev:proxy
# HTTPS_PROXY="http://127.0.0.1:7890"
# HTTP_PROXY="http://127.0.0.1:7890"