# 本地开发环境配置（优先级最高）
# 此文件会覆盖 .env.local 中的配置，不会被 Vercel CLI 影响

# 本地开发的 Auth URL
AUTH_URL="http://localhost:2011"

# 本地开发的代理配置（如不需要可以注释掉）
HTTPS_PROXY="http://127.0.0.1:7890"
HTTP_PROXY="http://127.0.0.1:7890"
